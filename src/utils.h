#ifndef UTILS_H
#define UTILS_H

/** Returns random double in range of 0...1 */
static inline double frand();

/** Returns random double in range of x_min...x_max */
static inline double random_double(double x_min, double x_max);

#endif
